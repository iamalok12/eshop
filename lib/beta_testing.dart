import 'package:flutter/material.dart';

class BetaTesting extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Thank you beta testing is over"),
      ),
    );
  }
}
